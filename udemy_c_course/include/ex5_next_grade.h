#ifndef EX5_NEXT_GRADE_H
#define EX5_NEXT_GRADE_H

#include <stdbool.h>

void next_grade();
bool check_mark(float mark);
char * choose_message (float average);
char * final_mesage (int fails, float average);

#endif
